[phases.setup]
nixPkgs = ["python311"]

[phases.install]
cmds = ["pip install -r morphyverse-server/requirements.txt"]

[start]
cmd = "cd morphyverse-server && uvicorn main:app --host 0.0.0.0 --port $PORT"
